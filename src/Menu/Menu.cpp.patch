--- src/Menu/Menu.cpp
+++ src/Menu/Menu.cpp
@@ -43,7 +43,7 @@
 
 std::string Menu::GetTextBuffer() { return this->textInputBoxBuffer; }
 
-#define STD_TO_CLAY(str) (Clay_String){ (int)(str).length(), (str).c_str() }
+#define STD_TO_CLAY(str) (Clay_String){ .isStaticallyAllocated = false, .length = (int)(str).length(), .chars = (str).c_str() }
 
 // go through all parts of the menu
 void Menu::DisplayMenu(std::unique_ptr<Neo> &currentNeo) {
@@ -62,17 +62,20 @@
 
   CLAY(CLAY_ID("MenuPanel"), {
     .layout = {
-        .layoutDirection = CLAY_TOP_TO_BOTTOM,
         .sizing = { .width = CLAY_SIZING_FIXED(400), .height = CLAY_SIZING_FIT() },
         .padding = {16, 16, 16, 16},
-        .childGap = 8
+        .childGap = 8,
+        .childAlignment = {CLAY_ALIGN_X_LEFT, CLAY_ALIGN_Y_TOP},
+        .layoutDirection = CLAY_TOP_TO_BOTTOM
     },
     .backgroundColor = {102, 191, 255, 180},
-    .border = { .width = {2, 2, 2, 2}, .color = {0, 0, 255, 255} },
-    .floating = { .attachTo = CLAY_ATTACH_TO_PARENT, .offset = {10, 10} }
+    .floating = { .offset = {10, 10}, .zIndex = 1, .attachPoints = {CLAY_ATTACH_POINT_LEFT_TOP, CLAY_ATTACH_POINT_LEFT_TOP}, .pointerCaptureMode = CLAY_POINTER_CAPTURE_MODE_PASSTHROUGH, .attachTo = CLAY_ATTACH_TO_PARENT },
+    .border = { .color = {0, 0, 255, 255}, .width = {2, 2, 2, 2} }
   }) {
-      Clay_TextElementConfig textTitle = { .fontSize = 20, .textColor = {0, 0, 0, 255} };
-      Clay_TextElementConfig textWhite = { .fontSize = 18, .textColor = {255, 255, 255, 255} };
+      Clay_TextElementConfig textTitle = { .textColor = {0, 0, 0, 255}, .fontId = 0, .fontSize = 20, .letterSpacing = 0, .lineHeight = 0, .wrapMode = CLAY_TEXT_WRAP_WORDS };
+      Clay_TextElementConfig textWhite = { .textColor = {255, 255, 255, 255}, .fontId = 0, .fontSize = 18, .letterSpacing = 0, .lineHeight = 0, .wrapMode = CLAY_TEXT_WRAP_WORDS };
+
+      // .userData, .textColor, .fontId, .fontSize, .letterSpacing, .lineHeight, .wrapMode
 
       CLAY_TEXT(CLAY_STRING("Asteroid Info"), CLAY_TEXT_CONFIG(textTitle));
 
